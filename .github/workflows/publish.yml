name: Publish Docker image

on:
  push:
    branches: [ main ]

jobs:
  publish:
    if: "!startsWith(github.event.head_commit.message, '[ci-skip]')"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get repository name in lowercase (no quotes)
        id: repo_lower
        uses: actions/github-script@v6
        with:
          script: |
            const repo = process.env.GITHUB_REPOSITORY.toLowerCase();
            core.setOutput("result", repo);

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.repo_lower.outputs.result }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ steps.meta.outputs.tags }}
            ghcr.io/${{ steps.repo_lower.outputs.result }}:latest
          labels: ${{ steps.meta.outputs.labels }}
